var characters = [],
		objects = [];

function ARCharacter(name, conversation) {
	this.name = name;
	this.conversation = conversation;
}


// ARCharacter.protype.speak()


// function character() handler

// function objects()

// object prototype



// function intiateCharacters

// var characterInfo = {
	// each character will have their own information
	// like:
	// -name; -object; -converstion; -flag to track interaction etc 

// }

// repeat for all characters

// push. and add to our character handler

// console loggin

//}

// initiateCharacters();
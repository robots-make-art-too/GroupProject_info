theme: jekyll-theme-hacker

defaults:
  -
    scope:
      path: "website/pages/" # an empty string here means all files in the project
      type: "pages" # previously `post` in Jekyll 2.2.
    values:
      layout: "default"